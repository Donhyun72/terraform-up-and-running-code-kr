output "address" {
  # value = "${aws_db_instance.example.address}"
  value = aws_db_instance.example.address
}

output "port" {
  # value = "${aws_db_instance.example.port}"
  value = aws_db_instance.example.port
}
